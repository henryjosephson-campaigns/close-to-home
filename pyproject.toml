[project]
name = "close-to-home"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.8",
    "httpx>=0.28.1",
    "shapely>=2.0.7",
    "uvicorn>=0.39.0",
]

[project.scripts]
close-to-home = "close_to_home:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
